# (optional) Horror film review title

Paste a horror review or essay below the line.
When empty, `horror-daily` tries Bloody Disgusting RSS; if that also fails it
skips (`source.optional: true`).

---
